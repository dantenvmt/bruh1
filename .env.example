# API Configuration
# Local development backend URL
VITE_API_URL=http://localhost:8000

# Note: In production, API_URL is injected at runtime via window.__CONFIG__
# This env var is only used during local development
